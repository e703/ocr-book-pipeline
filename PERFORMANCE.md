# 性能测试报告（本机实测）

测试主机：Intel Core i7-6650U（2C4T @2.2GHz，2015 年 15W 低压 U，无独显）/ 15GB 内存 / NVMe / Docker 29.8.0
测试日期：2026-09-07；软件：paddle 3.0.0（及 3.3.1 对照）、paddleocr 3.7.0、PyMuPDF 1.28、OpenCV 4.10

## 1. 镜像与部署资源

| 项目 | 数值 |
|---|---|
| paddlepaddle/paddle:3.0.0 拉取 | 压缩约 1.8GB，落盘 6.95GB，耗时约 6 分钟 |
| 3.3.1 落盘 | 6.99GB |
| 模型缓存（v4/v5/v6 全量） | 342MB，挂载持久化后重启秒级 |
| 单进程内存峰值 | 约 0.84-0.96GB |
| CPU 利用率（单进程 OCR） | 200-390%（多线程） |

## 2. 识别延迟与准确率（自建 4 页 A4 中英混合测试样本，1386 字符）

| 配置 | 镜像 | MKLDNN | 每页 OCR | 4 页合计 | 字符召回率 |
|---|---|---|---|---|---|
| PP-OCRv4 mobile（默认） | 3.0.0 | 开 | 1.3-1.9s | 6.0s | 0.972 |
| PP-OCRv5 mobile | 3.3.1 | 关 | 6.1-9.7s | 32.5s | 0.975 |
| PP-OCRv5 server（v5+中文默认） | 3.0.0 | 开 | 7.6-8.3s | 31.9s | 0.975 |
| PP-OCRv6 medium（开箱默认） | 3.3.1 | 关 | 14.7-20.9s | 71.8s | 0.977 |

结论：**3.0.0 镜像 + PP-OCRv4 mobile + MKLDNN 开** 是本机最优组合（1.5-2s/页），
精度与慢 5-10 倍的高阶模型几乎无差。

## 3. 图文混排样本（3 页：照片+图内标注 / 柱状图+图内文字 / 整页扫描件）

| 配置 | 每页 OCR | 3 页合计 | 平均召回率 |
|---|---|---|---|
| v4 mobile @200dpi | 1.5-2.2s | 5.4s | 0.989 |
| v5 server @200dpi | 8.9-10.5s | 29.3s | 0.994 |

DPI 对比（v4 mobile）：150dpi=5.1s/0.990，200dpi=5.4s/0.989，300dpi=8.9s/0.990
→ 渲染 150-200dpi 足够，300dpi 耗时 +75% 精度零提升。PDF 渲染本身仅 0.1-0.4s/页。
图内中文标注、图表数值识别正常；扫描件页是相对最弱点（0.987）但仍达标。

## 4. 服务化部署（paddlex --serve，FastAPI）

- 端点：GET /health，POST /ocr（JSON：`{file: base64或URL, fileType: 0=PDF 1=图片}`）
- 实测：3 页混合 PDF 整请求（含 30MB base64 双向传输+解码）6.0s；纯推理 1.5-2s/页
- 默认单请求最多 10 页（静默截断，含 dataInfo 谎报）；配置
  `Serving.extra.max_num_input_imgs: null` 解除后实测 12 页全量返回（24.8s）
- 大响应优化：`Serving.visualize: false` 关闭内联结果图（省 80%+ 响应体积）
- 单 worker 串行；并发建议 ≤2 实例，CPU 是唯一瓶颈

## 5. 扫描书管线（《Word的排版艺术》侯捷，414 页扫描 PDF，45.7MB）

预处理：内嵌原图直取（不重渲染）→ 高斯+CLAHE 增强 → 投影法去倾斜（±6° 搜索）
→ PP-OCRv4 mobile 识别（200dpi 等效，doc orientation 纠 90/180°）→ 读取顺序排序
（横排/竖排/多栏自适应）→ 重建可搜索 PDF（150dpi 去噪背景 + 不可见文字层 + 字体子集化）

| 指标 | 数值 |
|---|---|
| 单页耗时（单进程，cpu_threads=10） | 约 3.8s/页（OCR 为主） |
| 20 页预览 | 76.8s，输出 1.85MB（92KB/页） |
| 全书 414 页（4 进程并行，每进程 cpu_threads=1） | 实测墙钟约 28 分钟（4 段各 27-28 分钟并行），4 段输出 8.3-8.9MB/100 页 |
| 全书合并产物 | 34.7MB（84KB/页），可搜索文字层 304,983 字符，空白页警告 0 |
| 文字层可提取字符 | 全书约 30.5 万字符；抽查正文页顺序正确、误识率低 |
| 倾斜修正 | 0~1.25°（原扫描基本端正） |
| 已知瑕疵 | 封面艺术字、CIP 版权页多栏小块信息顺序略乱、"自己"→"自已" 类个别字误识 |

### 并行实测教训（重要）
4 进程 × cpu_threads=1 并行时，i7-6650U（15W）4 核满载触发功耗/散热降频：
单页 OCR 从 6.3s（单进程独占）劣化到 15-30s，并行总墙钟 ≈ 串行。**此类低压
CPU 上建议 2 进程 × 2 线程 或干脆 1 进程 × 全线程**；性能敏感场景应优先
换标压/服务器 CPU 或加 GPU。

## 6. 踩坑记录（复现避坑）

1. v6 模型在 paddle 3.0.0 镜像无法加载（PIR strides 属性错误）→ 需 3.1+ 镜像
2. paddle 3.3.1 镜像 MKLDNN 对所有模型崩溃（ConvertPirAttribute2RuntimeAttribute）
3. 自部署 API `fileType` 语义：0=PDF、1=图片，传错报 "Invalid input file"
4. 单请求 10 页静默截断（见第 4 节）
5. 噪声扫描件 Otsu 全局阈值在 116↔208 间跳变 → 二值化直接喂 OCR 会 0 行检测
6. Hough 直线法在噪声图误检 ±45° → 改用投影轮廓法（多角度旋转打分）
7. PyMuPDF 内置 CJK 字体 china-s 对 ASCII 提取不完整（丢字母）→ 用真实字体+子集化
8. 每页单独建文档合并 PDF 会让字库重复嵌入（6MB/页）→ 单文档内建页 + subset_fonts()

## 7. 测试产物

- 测试样本：pipeline/make_pdf*.py 生成（纯文本/图文混排两套，含 ground truth）
- 基准数据：pipeline/bench2.py 运行产出（支持 PDF/GT/DPI/配置过滤环境变量）
- 书处理产物：/home/alan/workspace/book/targets/（PDF、逐页文本、对比图、日志）
